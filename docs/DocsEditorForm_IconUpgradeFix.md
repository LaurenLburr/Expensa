# DocsEditorForm Icon Upgrade Fix

## Fixes

- Restores all designer field declarations so `DocsEditorForm.cs` can compile.
- Keeps the WinForms design-time split.
- Uses generated 16x16 toolbar icons at runtime.
- Avoids external icon files and resource dependencies for now.
