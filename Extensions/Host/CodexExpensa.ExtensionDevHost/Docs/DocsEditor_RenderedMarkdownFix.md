# Docs Editor Rendered Markdown Fix

## Changes

- The main display is now rendered Markdown using a browser preview.
- Raw Markdown source is hidden by default.
- Toolbar has a larger wrapping area so buttons are not clipped.
- `Show Markdown Source` displays the editable source pane.
- Markdown toolbar buttons reveal source automatically when editing is needed.
