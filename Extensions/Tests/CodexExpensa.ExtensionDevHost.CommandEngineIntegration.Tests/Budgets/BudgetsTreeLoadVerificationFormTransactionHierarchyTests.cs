namespace CodexExpensa.ExtensionDevHost.CommandEngineIntegration.Tests.Budgets;

// The former source-text test in this file was removed as stale.
//
// It required a specific CTE string, "WITH [SelectedBudgetRows] AS", to appear
// directly in the WinForms class. SQL placement and query shape have changed;
// tests should validate returned rows and hierarchy behavior rather than one
// historical SQL spelling embedded in a form.
