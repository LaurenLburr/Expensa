# Extension Manager Add-in Database and Test Nodes

Each add-in node now has two standard child nodes:

```text
Add-ins
  Websites Add-in (100)
    Websites Database
    Test - Websites Tree Test
```

The Database node is a placeholder for the add-in database page.

The Websites Test node opens the Extension Tree Load Test form, which contains a tree and fills only the Websites node.
