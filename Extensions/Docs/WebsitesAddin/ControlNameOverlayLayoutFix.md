# Control Name Overlay Layout Fix

The control-name toggle is repositioned at runtime so the tiny diagnostics button stays in the upper-right corner without disturbing the main form layout.

The stale ellipsis tests were also updated because the current UI no longer depends on Designer-only `AutoEllipsis` behavior.
