# Database Label Ellipsis

The runtime database link label now uses AutoEllipsis so long database names are shortened visually instead of stretching the layout.

The full path still appears in the path textbox below the label.
