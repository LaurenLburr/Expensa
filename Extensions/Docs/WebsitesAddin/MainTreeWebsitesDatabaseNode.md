# Main Tree Websites Database Node

The main Extension Dev Host tree now routes the Websites add-in Database node to:

```csharp
WebsitesDatabasePanelForm
```

Expected UI path:

```text
Add-in Projects
  WebsitesAddin
    Database
```

Selecting `Database` opens the Websites Database panel.

The `Test` node still opens the Extension Tree Load Test form.
