# Docs Editor True Preview Fix

## Fixes

- Toolbar row is taller and wraps buttons without clipping.
- Default view is rendered Markdown, not raw Markdown.
- Raw source is hidden unless `Show Markdown Source` is checked.
- Added `Normalize` button to repair documents that were previously saved as one long line.
