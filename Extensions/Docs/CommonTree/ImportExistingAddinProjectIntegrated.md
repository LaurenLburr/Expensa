# Import Existing Add-in Project

Adds an Add-in Projects node named `Import Existing Add-in Project...`. Browse to a `.csproj`; the service derives project name, target framework, assembly name, and relative bin path, then registers the add-in through the existing registration store.
