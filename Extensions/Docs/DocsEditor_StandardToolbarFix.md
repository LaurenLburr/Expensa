# Docs Editor Standard Toolbar Fix

## Fixes

- Replaces button soup with two standard ToolStrip rows.
- Prevents toolbar clipping.
- Keeps rendered Markdown as the default view.
- Keeps source hidden unless Show Markdown Source is toggled.
