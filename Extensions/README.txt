This is a STARTER package only.

It contains empty/basic projects and a starter solution layout.
If you extract it over an existing Extensions repo, files with the same names
will be replaced.

Extract somewhere else first if you only want to inspect it.
